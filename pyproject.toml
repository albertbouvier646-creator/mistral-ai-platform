[project]
name = "mistral-ai-platform"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "fastapi==0.115.0",
    "httpx==0.27.2",
    "pydantic==2.9.2",
]

[tool.vercel]
entrypoint = "api.chat:app"
